# تأمين قاعدة بيانات ميزان الشمس

## 🔴 إن كان الحفظ متوقّفاً الآن — افعل هذا فوراً

عدّاد ⏱ لا يفرغ ونقطة الاتصال خضراء؟ السيرفر يرفض الحفظ. أعِد فتح الباب حالاً،
وبياناتك المعلّقة ستُرسل تلقائياً:

Firebase Console ← Realtime Database ← Rules ← الصق محتوى **`firebase-rules-open.json`**
← Publish.

ثم افتح التطبيق ← الأدوات ← **📡 فحص الاتصال والصلاحيات** ← تأكّد أن الطابور فرغ.

بعدها ارجع لهذا الدليل وطبّق الحماية بالترتيب الصحيح. لا تتركها مفتوحة طويلاً.

---

## ⚠️ اعتذار وتصحيح — سبب العطل

النسخة الأولى من `firebase-rules.json` كانت تحوي هذا الشرط:

```
root.child('closures').child(newData.child('mk').val()).exists()
```

وحين لا يوجد الحقل `mk` تصير قيمته `null`، و**`child(null)` خطأٌ في لغة قواعد
فايربيس** فيُرفض الشرط كلّه. فكان يرفض:

- كل عملية **حذف** (لأن `newData` غير موجود أصلاً)
- كل سجل **بلا تاريخ**
- و**كل شيء** إن كانت النسخة المنشورة أقدم من v17.50 (لا تكتب `mk` إطلاقاً)

القاعدة الجديدة لا تعتمد على `mk` إطلاقاً. وقفل الفترات يبقى محروساً في التطبيق
كما هو — وهو الحارس الذي جرى اختباره فعلياً.

---

## الترتيب الصحيح للتنفيذ

⚠️ **عكس هذا الترتيب يوقف الحفظ.**

### ١) فعّل الدخول المجهول

Firebase Console ← **Authentication** ← Sign-in method ← **Anonymous** ← Enable.

**هذه أكثر خطوة تُنسى.** بدونها كل قاعدة تشترط `auth != null` سترفض كل شيء —
ونقطة الاتصال تبقى خضراء لأن `‎.info/connected` لا يحتاج صلاحية، فيبدو كل شيء
سليماً وهو ليس كذلك.

### ٢) انشر نسخة التطبيق v17.57 أو أحدث

هي التي تسجّل الدخول المجهول، وتُظهر سبب الرفض بدل رسالة «محفوظ محلياً»،
وفيها أداة الفحص.

### ٣) تحقّق قبل نشر القواعد

الأدوات ← **📡 فحص الاتصال والصلاحيات** ← يجب أن تكون كل السطور ✓،
وخصوصاً **«تسجيل الدخول (هوية الجلسة)»**. إن كان ✗ فارجع للخطوة ١.

### ٤) انشر القواعد

الصق محتوى `firebase-rules.json` ← Publish.

### ٥) اختبر فوراً

- شغّل الفحص ثانيةً → كل السطور ✓ والطابور فارغ.
- احفظ وصلاً جديداً → يختفي عدّاد ⏱ خلال ثوانٍ.
- **جرّب حذف وصل تجريبي** → يجب أن ينجح (هذا ما كان مكسوراً).
- افتح الرابط في نافذة تصفّح خاصّ → يعمل أيضاً.

إن تعطّل شيء: ارجع إلى `firebase-rules-open.json`، ثم راجع الفحص.

---

## ما الذي تحميه القواعد

| الحماية | الأثر |
|---|---|
| `auth != null` على كل شيء | يوقف المسح العشوائي لقواعد فايربيس المكشوفة — أشيع خطر فعلي |
| `audit_log` إضافة فقط | لا يستطيع أحد — ولا التطبيق — تعديل قيد تدقيق أو محوه |
| `.validate` على السجلات | يمنع كتابة بيانات مشوّهة تُفسد الشاشات |
| `.read/.write = false` في الجذر | أي عقدة جديدة مرفوضة حتى تُسمح صراحةً |

**ملاحظة مقصودة**: `.validate` مكتوبة `!newData.exists() || …` كي لا تمنع الحذف.
هذا بالضبط ما نسيتُه في النسخة الأولى.

---

## ما لا تحميه — بصراحة

الدخول المجهول يعني أن **كل من يملك رابط التطبيق يحصل على هوية**. فهو يوقف من
يمسح الإنترنت بحثاً عن قواعد مكشوفة، ولا يوقف من أعطيتَه الرابط ثم أردت سحبه.

### الطبقة التالية — حساب لكل مستخدم

بها وحدها تستطيع **سحب صلاحية شخص بعينه**:

1. Authentication ← Users ← أضف حساباً لكل مستخدم (بريد + كلمة سر).
2. في التطبيق: بدّل `signInAnonymously()` إلى `signInWithEmailAndPassword()`.
   رمز الـ PIN يبقى للسرعة في الساحة، لكن ما يفتح قاعدة البيانات هو الحساب.
3. أضف عقدة `roles/$uid` وشدّد القواعد لتقرأ منها.

من ترك العمل → تحذف حسابه → تُغلق كل الأبواب في لحظة.

### قفل الفترات على الخادم (اختياري ومتقدّم)

أزلتُه من القواعد الافتراضية بعد ما حدث: مكسبه صغير وخطره — كما رأيت — كبير.
القفل يعمل في التطبيق ومختبَر. إن أردته على الخادم لاحقاً فالشرط الصحيح
**null-safe** هكذا:

```
".write": "auth != null
  && (!newData.exists() || !newData.hasChild('mk') || !root.child('closures').child(newData.child('mk').val()).exists())
  && (!data.exists()    || !data.hasChild('mk')    || !root.child('closures').child(data.child('mk').val()).exists())"
```

لا تطبّقه إلا بعد أن تتأكّد أن كل سجلاتك اكتسبت الحقل `mk` (يُكتب تلقائياً عند
أول تعديل على كل سجل في v17.50+).

### وأمور أخرى

- **نسخة احتياطية خارج فايربيس**: القواعد تحمي من الغريب لا من الخطأ. استمرّ في
  تصدير ملف النسخة واحفظه خارج الهاتف.
- **حدود الاستهلاك**: Firebase Console ← Usage ← اضبط تنبيهاً.
