# تأمين قاعدة بيانات ميزان الشمس

## الوضع الحالي — اقرأ هذا أولاً

`FB_CONFIG` في ملف التطبيق مكشوف للجميع، وهذا **طبيعي ومقبول** في فايربيس: هو
عنوان لا مفتاح. الحماية الحقيقية ليست هناك، بل في **قواعد الأمان** على الخادم.

إن كانت قواعدك اليوم:

```json
{ "rules": { ".read": true, ".write": true } }
```

فأي شخص يفتح رابط التطبيق يستطيع **قراءة حساباتك كلها ومحوها** من أدوات
المتصفح في دقيقة. ورموز الدخول الأربعة مكتوبة في الملف نفسه، فهي تمنع
الفضولي لا من يعرف.

**افحص الآن**: Firebase Console ← Realtime Database ← Rules.

---

## الترتيب الصحيح للتنفيذ — لا تعكسه

⚠️ **لو نشرت القواعد قبل تفعيل الدخول المجهول، سيتوقّف التطبيق عن الحفظ فوراً.**
اتبع الترتيب:

### ١) فعّل الدخول المجهول

Firebase Console ← **Authentication** ← Sign-in method ← **Anonymous** ← Enable.

### ٢) انشر نسخة التطبيق الجديدة (v17.50 أو أحدث)

هي التي تسجّل الدخول المجهول وتكتب حقل `mk`.

### ٣) تأكّد أن المصادقة تعمل

افتح التطبيق ← يجب أن تكون **نقطة الاتصال خضراء**، وألّا يظهر تنبيه
«فعّل Anonymous». احفظ وصلاً تجريبياً وتأكّد أنه وصل لجهاز آخر.

### ٤) شغّل التطبيق يوماً واحداً قبل القواعد

حتى تكتسب السجلات الموجودة حقل `mk` عند أول تعديل عليها. السجلات القديمة
بلا `mk` تبقى قابلة للتعديل (القاعدة تسمح بها) — وهذا مقصود كي لا تتعطّل
بياناتك التاريخية.

### ٥) الآن انشر القواعد

الصق محتوى `firebase-rules.json` في Rules ← **Publish**.

### ٦) اختبر فوراً بعد النشر

- احفظ وصلاً جديداً → يجب أن ينجح.
- افتح الرابط في نافذة تصفّح خاصّ → يجب أن يعمل أيضاً (يسجّل دخولاً مجهولاً جديداً).
- أغلق شهراً من «📜 سجلّ التدقيق» ثم حاول تعديل وصل فيه → يجب أن يُرفض.

إن تعطّل شيء: أعد القواعد إلى `{"rules":{".read":true,".write":true}}` مؤقتاً،
وراجع الخطوة ١.

---

## ما الذي تحميه هذه القواعد

| الحماية | الأثر |
|---|---|
| `auth != null` على كل شيء | يوقف المسح العشوائي لقواعد فايربيس المكشوفة — وهو أشيع خطر فعلي |
| `audit_log` إضافة فقط | لا يستطيع أحد — ولا التطبيق نفسه — تعديل قيد تدقيق أو محوه |
| `cash_counts` إضافة فقط | لا يُعدَّل جرد صندوق بعد تسجيله |
| قفل الفترات على الخادم | الشهر المغلق محميّ حتى لو عُبث بالواجهة |
| `.validate` على كل سجل | يمنع كتابة بيانات مشوّهة تُفسد الشاشات |
| `.read/.write = false` في الجذر | أي عقدة جديدة مرفوضة افتراضياً حتى تُسمح صراحةً |

---

## ما لا تحميه — كن صريحاً مع نفسك

الدخول المجهول يعني: **كل من يملك رابط التطبيق يحصل على هوية**. فهو يوقف
الغريب الذي يمسح الإنترنت بحثاً عن قواعد مكشوفة، ولا يوقف من أعطيتَه الرابط
ثم أردت سحبه منه.

### الطبقة التالية — حساب لكل مستخدم

هذه هي الحماية الحقيقية، وبها وحدها تستطيع **سحب صلاحية شخص بعينه**:

1. Authentication ← Users ← أضف أربعة حسابات بريد/كلمة سر (واحداً لكل مستخدم).
2. في التطبيق: بدّل `signInAnonymously()` إلى `signInWithEmailAndPassword()`،
   واربط كل مستخدم في `USERS` بحسابه. رمز الـ PIN يبقى كما هو للسرعة في
   الساحة، لكن ما يفتح قاعدة البيانات هو الحساب لا الرمز.
3. أضف عقدة `roles/$uid` وشدّد القواعد:

```json
"records": {
  ".read": "auth != null",
  "$id": { ".write": "root.child('roles').child(auth.uid).child('write').val() === true && ..." }
}
```

عندها: من ترك العمل → تحذف حسابه → تُغلق كل الأبواب في لحظة، بلا تغيير في
التطبيق ولا في الرموز.

### وأمور أخرى

- **نسخة احتياطية خارج فايربيس**: القواعد تحمي من الغريب، لا من خطأ منك.
  استمرّ في تصدير ملف النسخة واحفظه خارج الهاتف.
- **حدود الاستهلاك**: Firebase Console ← Usage ← اضبط تنبيهاً، كي تنتبه لأي
  استهلاك غير معتاد.
