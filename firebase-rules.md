# قواعد أمان فايربيس — ميزان الشمس

## ⚠️ اقرأ هذا أولاً عند إضافة أي قسم جديد

قواعد فايربيس **قائمة بيضاء**: كل قسم في التطبيق يكتب في عقدة خاصة به،
وما لم تُذكر العقدة صراحةً في القواعد **يرفض السيرفر كل ما يُكتب فيها**.

وشكل العطل مُضلِّل: الحفظ ينجح على الشاشة (الكتابة متفائلة: تُحدَّث
النسخة المحلية فوراً)، ثم يرفضه السيرفر، وعند أول تحديث يعود المستمع
بالبيانات القديمة فتختفي التعديلات — فيبدو أن «التعديل لا يعمل».

**لذلك: أي قسم جديد ⇦ أضف عقدته هنا وفي لوحة فايربيس قبل استعماله.**

---

## إصلاح سريع: قسم مرفوض

افتح التطبيق ← **الأدوات** ← **📡 فحص الاتصال والصلاحيات**. يُسمّي لك
كل قسم رفضه السيرفر واسم عقدته بالضبط.

ثم في [لوحة فايربيس](https://console.firebase.google.com) ←
**Realtime Database** ← **Rules**:

**أسهل طريقة وأأمنها** — انسخ كتلة `"arb_records"` الموجودة عندك،
والصق نسخةً منها، وغيّر اسمها فقط إلى اسم العقدة الناقصة. القسمان
متطابقان في الشكل (أوزان بلا مبالغ)، فالقاعدة نفسها تصلح لهما.

```jsonc
"arb_records": { ... },
"out_records": { ... }   // ← نسخة طبق الأصل باسم جديد
```

ثم **Publish**. يسري المفعول خلال ثوانٍ بلا إعادة تشغيل التطبيق.

---

## الملف الكامل

`firebase-rules.json` في هذا المستودع نسخة كاملة مولَّدة من العقد التي
يستعملها التطبيق فعلاً. استعملها إن أردت استبدال قواعدك كلها.

> ⚠️ إن كانت قواعدك الحالية تحوي قيوداً إضافية خاصة بك، فالاستبدال
> الكامل يمحوها. عندها الأفضل الطريقة السريعة أعلاه.

### ما تضمنه هذه القواعد

| الطبقة | الحكم |
|---|---|
| **الهوية** | `auth != null` على كل قراءة وكتابة — لا أحد بلا هوية |
| **الجذر** | `.read`/`.write` = `false` — أي عقدة غير مذكورة مرفوضة |
| **الفترات المغلقة** | الشهر المغلق تُرفض فيه الكتابة **على الخادم** لا في الواجهة وحدها |
| **سجلّ التدقيق** | `audit_log` يُضاف إليه ولا يُعدَّل ولا يُحذف — ولا حتى من التطبيق |

### العقد المشمولة

**أقسام بتاريخ** (محروسة بالفترات المغلقة):
`records` · `sell_records` · `dam_records` · `srf_records` · `wrk_records`
· `mnl_records` · `arb_records` · `out_records` · `sal_records`
· `adv_records` · `emp_txns`

**أقسام بلا تاريخ:**
`emp_list` · `cashbox` · `cash_boxes` · `cash_counts` · `cash_moves`

**خاصة:** `closures` · `audit_log` (إضافة فقط) · `__conncheck__` (عقدة الفحص)

---

## حارس الفترات المغلقة — لماذا حقل `mk`

قواعد فايربيس لا تستطيع اقتطاع جزء من نصّ، فلا يمكنها قراءة الشهر من
`"2026-09-21"`. لذلك يكتب `_fbWrite` حقلاً مشتقّاً `mk = dk.slice(0,7)`
مع كل سجل، فتسأل القاعدة عنه مباشرة:

```
root.child('closures').child(newData.child('mk').val()).child('open').val() == true
```

ويُفحص الشهر القديم والجديد معاً، وإلا أمكن نقل سجل **من** شهر مغلق.

> `mk` حقل مشتقّ يكتبه التطبيق وحده. أي سجل بلا `mk` (أي بلا تاريخ)
> يمرّ بلا حراسة — وهذا مقصود لأقسام كالعمال والصندوق.

---

## الطبقة التالية: حساب لكل مستخدم

الدخول المجهول يُغلق الباب أمام المسح العشوائي، لكنه لا يفرّق بين شخص
وآخر — ولا يمكن سحب صلاحية من ترك العمل. لذلك يدعم التطبيق ربط كل جهاز
بحساب بريد وكلمة سر (انظر «ربط الجهاز» في الأدوات).

الانتقال بلا تعطيل: ما دام **Anonymous** مُفعَّلاً يعمل الجميع كما هم؛
ومن ربط جهازه صارت هويته هي المستعملة. فإذا ربط الجميع أجهزتهم، أطفئ
Anonymous من لوحة فايربيس فيصير الحساب إلزامياً — بلا يوم توقّف واحد.

> 🔑 كلمة السر لا تُكتب في ملف التطبيق ولا تُحفظ في التخزين المحلي.
> تُدخل مرة على كل جهاز، وفايربيس يحتفظ بالجلسة بنفسه.
