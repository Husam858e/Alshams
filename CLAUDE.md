# ميزان الشمس — دليل الكود

نظام إدارة وحسابات ساحة وزن ومخازن. **ملف HTML واحد** يُنشر بنسخه، ويعمل دون
إنترنت. يُستعمل في حسابات مالية حقيقية — **لا مجال للخطأ الصامت**.

```
wheelmanagement_v17_promax_40.html   التطبيق كلّه (‎~16,900 سطر)
tests/                                27 مجموعة اختبار على متصفح حقيقي
firebase-rules.json                   قواعد الأمان
firebase-rules-open.json              فتح طارئ للاستعادة الفورية
firebase-rules.md                     ترتيب التنفيذ — عكسه يوقف الحفظ
```

## قبل أي تعديل

```bash
npm test        # 27 مجموعة · 270+ فحصاً · ‎~100 ثانية
```

**لا تنشر بلا اختبار.** كل مجموعة موجودة لأن عطلاً حقيقياً وقع؛ راجع
`tests/README.md`.

---

## البنية

ملف واحد بثلاث كتل: `<style>` ثم `<script>` (كل المنطق) ثم `<body>` (كل الشاشات).
لا build ولا مكتبات محلية. الخارجية (Excel · PDF · فايربيس) تُحمَّل **عند الطلب**
بخوادم بديلة — فالتطبيق يفتح فوراً بلا إنترنت.

### نقطة الاختناق — أهم دالة في الملف

```js
_fbWrite(node, id, data, onLocal)
```

**كل** كتابة تمرّ منها: إنشاء وتعديل وحذف، في كل الأقسام. وفيها بالترتيب:

1. تنقية النصّ ونزع `undefined`
2. حقل `mk` المشتقّ (شهر التاريخ)
3. التقاط الحالة السابقة — **قبل** `onLocal`
4. حارس الفترات المغلقة
5. تحديث الكاش المحلي (`onLocal`)
6. قيد سجلّ التدقيق
7. الطابور دون اتصال ثم الإرسال

> ضع أي حارس أو أثر جديد **هنا** لا في دوال الحفظ الاثنتي عشرة — وإلا نُسيت
> واحدة. أي قسم يُضاف مستقبلاً يرث كل ذلك مجاناً.

### جدول المجموعات

`_DATASETS` يصف كل مجموعة بيانات: مفتاحها، عقدتها في فايربيس، حقول البحث،
دالة المبلغ، وصاحبها (`who`). البحث الشامل والنسخ الاحتياطي وفحص السلامة
والسلة كلها مبنيّة عليه — أضف صفاً واحداً يعمل كل ذلك.

---

## اصطلاحات لا تُكسر

### ⚠️ مفاتيح المواقع تاريخية

```js
const YARD = "ابو ابراهيم";        // مفتاح «الساحة» — لا يُغيَّر أبداً
WH_LABELS["ابو ابراهيم"] = "الساحة";
WH_LABELS["مخزن ابو ابراهيم"] = "ابو ابراهيم";
```

القيمة المخزّنة للساحة منذ البداية هي `"ابو ابراهيم"`، وكل السجلات القديمة
تحملها. **غيّر التسمية المعروضة فقط، لا المفتاح** — وإلا انفصلت سجلات سنوات.

### الأسماء العربية

- `normArabic` — همزات · تاء مربوطة · تشكيل · «ال» التعريف
- `nameKey` — مفتاح قانوني بلا مسافات؛ يوحّد «عبد الكريم» و«عبدالكريم»،
  ويقصّ ما بعد `+` لأن الشراكة تُنسب لأولها
- `smartMatch` — للبحث: **احتواء**، لا تطابق

> **`smartMatch` للبحث لا للدفع.** «عبد» يطابق «عبد الله» و«عبد الرحمن».
> الدفع يمرّ بالهوية: `_ownerMatches` + تجميع بالأشخاص + اختيار صريح عند
> التعدّد. هذا العطل وقع فعلاً وذهب مال شخص لغيره.

### التواريخ

`dk` نصّ `YYYY-MM-DD`. استعمل `_D()` للتحويل و`_ds()` للإخراج — **لا
`new Date(str)`** فيُفسَّر توقيتاً عالمياً ويسقط الأحد من الأسبوع في بغداد.
وقع هذا مرتين (`_jamiInPeriod` ثم `_filterByPeriod`).

### الأرقام

- `numIn(id)` للأوزان والأسعار — يزيل الفواصل والأرقام العربية، ويُبقي `NaN`
  للفراغ
- `payAmt(id)` للمبالغ — يُرجع `0` للفراغ
- **لا `parseFloat(el.value)`** — `parseFloat("2,400") = 2`

`unfmtNum` تُزيل إشارة السالب، فلا قيمة سالبة تدخل قاعدة البيانات إطلاقاً.

### التهريب

- `esc(v)` لكل ما يكتبه المستخدم
- مُساعد `row(k,v)` في بُناة الوصولات **يُهرّب قيمته** — لا تُهرّبها مرة أخرى
- `prRowRaw(k,html)` للوسم الموثوق وحده (أيقونة المادة)

### خطّ الأساس في الخزينة

```
fromDk = أول يوم تُحسب حركاته
  الرصيد الافتتاحي  → تاريخه نفسه
  الجرد المعتمد ليوم D → D+1   (لأنه رصيد نهاية D)
```

الخلط هنا يُزيح الحساب يوماً كاملاً.

---

## عقد فايربيس

| العقدة | المحتوى |
|---|---|
| `records` `sell_records` `dam_records` `srf_records` `wrk_records` `mnl_records` `arb_records` | الوصولات |
| `emp_list` `sal_records` `adv_records` `emp_txns` | العمال والرواتب |
| `audit_log` | سجلّ التدقيق — **إضافة فقط**، لا دالة محو في التطبيق |
| `closures` | الفترات المغلقة |
| `cash_boxes` `cash_moves` `cash_counts` `cashbox` | الخزينة |

---

## أخطاء وقعت — لا تُعِدها

| الخطأ | الدرس |
|---|---|
| الدفع الشامل يدفع ما يُرجعه البحث | البحث يطابق الملاحظة واللوحة والمخزن. **الدفع بالهوية** |
| `new Date(dk)` في حساب الأسبوع | `_D()` دائماً — وإلا سقط الأحد |
| قاعدة فايربيس `child(null)` | خطأ يرفض الكتابة كلّها؛ احرس بـ `hasChild` قبل `.val()` |
| تهريب `row()` شمل أيقونة المادة | ميّز الوسم الموثوق بدالة مستقلة |
| «استبدال كامل» في الاستعادة لا يحذف | إن وعد الزرّ بشيء فليفعله |
| الفائض بعد تخفيض وصل مدفوع | `getRemaining` تقصّ عند الصفر — استعمل `getOverpay` |
| تعديل الأوزان بقيمة غير صالحة | يُرفض، ولا يُحفظ وصل يناقض نفسه |

---

## النشر

انسخ ملف HTML إلى الاستضافة. لا build.

قواعد الأمان: اقرأ `firebase-rules.md` — **الترتيب مهمّ** (فعّل Anonymous أولاً،
تحقّق من الأدوات ← 📡 فحص الاتصال والصلاحيات، ثم انشر القواعد). عكسه يوقف الحفظ
بينما تبقى نقطة الاتصال خضراء.
